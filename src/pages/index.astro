---
import ColorCycleBackground from '../components/ColorCycleBackground.astro';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>Truchrome — Coming Soon</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        background: #0a0a0a;
        color: var(--fg, #ffffff);
        height: 100dvh;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .container {
        position: relative;
        z-index: 2;
        text-align: center;
        padding: 0 24px;
      }

      h1 {
        font-size: 48px;
        font-weight: 600;
        letter-spacing: -0.02em;
        margin-bottom: 16px;
      }

      p {
        font-size: 18px;
        color: var(--fg-muted, rgba(255, 255, 255, 0.72));
        margin-bottom: 32px;
      }
    </style>
  </head>
  <body>
    <ColorCycleBackground />
    <div class="container">
      <h1>TruChrome</h1>
      <p>Something is coming. Stay tuned.</p>
    </div>
  </body>
</html>
